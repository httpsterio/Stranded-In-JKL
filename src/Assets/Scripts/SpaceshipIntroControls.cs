using UnityEngine;
using System.Collections;

public class SpaceshipIntroControls : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GameManager.instance.Conversation (0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
